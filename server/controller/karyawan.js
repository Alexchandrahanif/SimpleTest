const { Karyawan } = require("../models/index");

class Controller {
  static async getAll(req, res, next) {
    try {
    } catch (error) {
      next(error);
    }
  }
}

module.exports = Controller;
